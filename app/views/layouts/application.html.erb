<!DOCTYPE html>
<html>
<head>
  <title>Spotlight</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <script src="https://kit.fontawesome.com/f77b02233a.js" crossorigin="anonymous"></script>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%#= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
  <header>
    <div class="container">
     <h2>Spotlight</h2>
      <div class="header-list">
        <ul>
          <% if user_signed_in? %>
            <li><%= link_to "マイページへ", user_path(current_user.id) %></li>
            <li><%= button_to 'ログアウト', destroy_user_session_path, method: :delete, class: 'logout-btn' %></li>
          <% else %>
            <li><%= link_to "新規登録", new_user_registration_path, class: 'signup-link' %></li>
            <li><%= link_to "ログイン", new_user_session_path, class: 'login-link' %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </header>

  <main>
    <%= yield %>
  </main>
</body>

</html>